# Kafka Configuration (Confluent Cloud)
KAFKA_BOOTSTRAP=your-kafka-bootstrap-servers
KAFKA_SASL_MECHANISM=PLAIN
KAFKA_SECURITY_PROTOCOL=SASL_SSL
KAFKA_SASL_USERNAME=your-kafka-username
KAFKA_SASL_PASSWORD=your-kafka-password

# Kafka Topics
KAFKA_ORDER_TOPIC=stream_order_producer
KAFKA_PAYMENT_TOPIC=stream_payment_producer

# MongoDB Connection (configured in join_stream.py)
# Update the MongoDB URI directly in join_stream.py
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database

# Google Cloud Storage (for logs and checkpoints)
# GCS_BUCKET=your-gcs-bucket-name
# GCS_CHECKPOINT_PATH=gs://your-bucket/checkpoints/
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Spark Configuration (Optional overrides)
# SPARK_MASTER=local[*]
# SPARK_APP_NAME=StatefulOrderPaymentStreaming
